# frozen_string_literal: true

# Not an application Gemfile. Load this gem from OpenProject's
# Gemfile.plugins. Rails and OpenProject::Plugins come from core —
# never add openproject-plugins (~> 1.0) or rails here.

source "https://rubygems.org"

gemspec
